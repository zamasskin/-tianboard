{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "start:api": "lerna run start --stream --scope @tianboard/api",
    "start:analytics": "lerna run start --stream --scope @tianboard/analytics",
    "start:prod": "lerna run start:prod",
    "bootstrap": "yarn install"
  }
}
