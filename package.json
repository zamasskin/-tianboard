{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "start:prod": "lerna run start:prod",
    "bootstrap": "yarn install"
  }
}
